type Props = {
    storeName:string,
    userName:string,
    email: string,
    password: string,
    remember: boolean
}

export const registerUser = async(data: Props)=>{
    // TODO : Register API
}